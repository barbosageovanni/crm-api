export const getUserById = async (id: string) => {
  // TODO: Implement logic to retrieve user data by ID from the database.
  // For now, return a dummy user object.
  return { id, username: 'dummyUser', email: 'dummy@example.com' };
};